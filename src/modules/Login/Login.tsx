import { OverLayout } from '../../components';
import { LoginForm } from './LoginForm';

const Login = () => {
  return (
    <OverLayout>
      <div>...Login...</div>
      <LoginForm />
    </OverLayout>
  );
};

export default Login;
