import { ThemeToggle } from '../ThemeToggle';
import { LocalesToggle } from '../LocalesToggle';

const Header = () => {
  return (
    <header>
      header
      <ThemeToggle />
      <LocalesToggle />
    </header>
  );
};

export default Header;
